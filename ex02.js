/** @format */

function sum(para1, para2, para3) {
  return para1 + para2 + para3;
}

var res1 = sum(20, 60, 90);

// 결과 임시 저장
console.log(res1);
// 결과 바로 출력
console.log("더한 결과 : " + sum(20, 60, 90));

function print(pa) {
  console.log("출력 데이터 : " + pa);
}
print("룰루랄라");
